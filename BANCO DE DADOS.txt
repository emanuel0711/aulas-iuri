create DATABASE loja;
	USE loja;
	
	CREATE TABLE clientes (
		id_cliente INT PRIMARY KEY,
		nome VARCHAR(100),
		email VARCHAR(100)
	);


CREATE TABLE IF NOT EXISTS pedidos (
    id_pedido INT PRIMARY KEY,
    descricao VARCHAR( 200),
    valor DECIMAL(10, 2),
    id_cliente INT,
    FOREIGN KEY (id_cliente) REFERENCES clientes(id_cliente)
    );


USE loja_emanuel

INSERT IGNORE INTO clientes(id_cliente, nome, email) VALUES
	(1,'emanuel chaves', 'emanuel@exemplo.com'),
    (2, 'stevao rodrigues', 'stevao@exemplo.com'),
    (3, 'pedro henrique', 'pedro@exemplo.com'),
    
    INSERT INTO pedidos(id_pedido, descricao, valor, id_cliente) VALUES
    	(101, 'compra moveis', 1500.00,1),
        (102, 'pedidos de eletronicos',2500.00, 2),
        (103, 'servicos de instalação', 500.75, 3);
    