var leitor= require("readline-sync")

class Pessoa{
    nome: string
    idade: number
    ano_exp: number

    constructor(nome: string, idade: number, ano_exp: number){
        this.nome = nome
        this.idade = idade
        this.ano_exp = ano_exp
    }
}

class Escola{
    pessoa: Pessoa
    nome_esc: string
    end_esc: string
    num_esc: number

    constructor(pessoa: Pessoa, nome_res: string, end_res: string, num_end: number){
        this.pessoa = pessoa
        this.nome_esc = nome_res
        this.end_esc = end_res
        this.num_esc = num_end
    }

    getProfessor(): void{
        console.log(`Olá, sou o professor ${this.pessoa.nome}, tenho ${this.pessoa.idade} e tenho  ${this.pessoa.ano_exp} `)   
    }

    setProfessor(): void{

    let nomeUp = leitor.question("Qual o nome do profesor?")
    let idadeUp = leitor.question("Qual o nome do profesor?")
    let ano_xpUp = leitor.question("Qual o nome do profesor?")

    this.pessoa.nome = nomeUp
    this.pessoa.idade = idadeUp
    this.pessoa.ano_exp = ano_xpUp

   }

   setEscola(): void{
    
        function atualizarEscola(): array<any>{
            let nome_escolaUpdate = leitor.question('qual o nome da escola?')
            let end_escolaUpdate = leitor.question('qual o nome da escola')
            let num_enderecoUpdate = leitor.questionInt('e o numero?')
            let atualizacaoEscola = [nome_escolaUpdate, end_escolaUpdate, num_enderecoUpdate]
            return atualizacaoEscola
        }
    }
}

let menu: boolean = true

let prof = new Professor('', 0, 0)
let escola = new Escola (prof, '', '', 0 )